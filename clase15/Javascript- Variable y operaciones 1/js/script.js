/* calcilo de volumen de paquetes para enviar

let anchoPaquete = prompt("Ancho: ");
let altoPaquete= prompt("Alto: ");

const profundidadPaquete = 10;

let volumen = anchoPaquete * altoPaquete * profundidadPaquete;
alert(" El volumen del paquete es" + volumen);

Calculo moneda */

let cantidadDolares = prompt("Cuantos dolares tenes: ");
let tipoCambio = 733;

const cambioMonedaLocal = cantidadDolares*tipoCambio;

alert("Tenees $"+cambioMonedaLocal+"en dolares");